name: M6 Format API
on: [push]
permissions:
  contents: write
jobs:
  format:
    if: github.ref == 'refs/heads/brain/task-007-editorial-workflows'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: brain/task-007-editorial-workflows
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install ruff==0.12.11
      - run: ruff format services/local-core/src/book_os_core/app.py services/local-core/tests/test_editorial_api.py
      - name: Commit formatting
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "book-os-ci"
          git config user.email "book-os-ci@users.noreply.github.com"
          git add services/local-core/src/book_os_core/app.py services/local-core/tests/test_editorial_api.py
          git commit -m "style: format M6 editorial API"
          git push origin HEAD:brain/task-007-editorial-workflows
